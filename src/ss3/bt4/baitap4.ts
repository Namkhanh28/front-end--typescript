let str: string = "2";     // kiểu string
let num: number = 2;       // kiểu number

// So sánh lỏng (==)
//console.log(str == num);   // true

// So sánh chặt (===)
//console.log(str === num);  // false
